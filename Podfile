# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GoneungLift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'SDWebImage'  
  pod 'SwiftyJSON'
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  
  
# Pods for GoneungLift

  target 'GoneungLiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GoneungLiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
